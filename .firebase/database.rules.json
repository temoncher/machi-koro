{
  "rules": {
    "lobbies": {
      "$lobbyId": {
        ".read": "auth !== null",
        ".write": "(!data.exists() && auth !== null && newData.child('hostId').isString()) || data.child('hostId').val() === auth.uid",
        "users": {
          "$userId": {
            ".validate": "newData.exists() && newData.child('userId').val() === auth.uid && newData.child('username').isString()",
            ".write": "data.parent().exists() && auth.uid === $userId"
          }
        }
      }
    }
  }
}