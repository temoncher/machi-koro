{
  "rules": {
    "lobbies": {
      "$lobbyId": {
        ".read": "auth !== null",
        ".write": "(!data.exists() && auth !== null) || (data.exists() && data.child('hostId').val() === auth.uid)",
        "users": {
          "$userId": {
            ".write": "auth.uid === $userId"
          }
        }
      }
    }
  }
}